name: raksha
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  geolocator: ^11.0.0
  geocoding: ^3.0.0
  file_picker: ^8.0.0
  google_maps_flutter: ^2.5.0
  location: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
